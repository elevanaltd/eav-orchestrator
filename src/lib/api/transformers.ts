/**
 * Transformation Layer API
 *
 * Re-exports transformation utilities from the types/editor module
 * Provides boundary layer transformations between database and UI models
 */

export { toUIModel, toApiModel } from '../../types/editor';
